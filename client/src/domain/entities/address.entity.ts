export type AddressProps = {
    cep: string,
    street: string,
    complement?: string,
    neighborhood: string,
    city: string,
    uf: string,
    number: string,
    lat: number,
    lng: number,
}